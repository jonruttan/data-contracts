version: 1
id: schema.registry.v1.assertions
type: assertions
fields:
  contract:
    type: any
    required: false
    since: v1
    description: Canonical assertion tree root mapping with defaults/steps; leaves are direct spec-lang operator mappings.
  contract.defaults.imports:
    type: mapping
    required: false
    since: v1
    description: Default assertion import bindings inherited by all steps.
  contract.steps[].imports:
    type: mapping
    required: false
    since: v1
    description: Step-level import bindings merged over contract.defaults.imports.
