# Reviews

This folder stores periodic review outputs for `spec_runner` (human or AI).

Use `TEMPLATE.md` for consistent structure.
Use `HARDENING_PIPELINE.md` for the pre-merge hardening workflow prompt.
