# Commit Message Guidelines

Same format as the main repo:

```text
<type>(<scope>): <subject>
```

Types: `feat`, `fix`, `refactor`, `docs`, `test`, `build`.

Subject:

- imperative, present tense
- no trailing period
- keep it short and specific

