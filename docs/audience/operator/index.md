# Operator Audience Docs

Audience-targeted documentation entries are rendered from executable spec metadata.

## [implicit_ids_v1.spec.md](/specs/03_conformance/cases/core/implicit_ids_v1.spec.md)
### implicit_ids_v1.doc.1.1.operator
- Location: `/specs/03_conformance/cases/core/implicit_ids_v1.spec.md`
- Summary: contract docs id intentionally omitted (operator)
- Description:
  Purpose: Capture actionable details on how this contract symbol behaves for the declared audience.
  
  Inputs:
  - Inputs come from the declared contract and export bindings for this symbol.
  - Runtime bindings and policy constraints are applied by the harness before evaluation.
  
  Returns:
  - The assertion or helper return value produced by this symbol.
  
  Errors/Caveats:
  - Malformed inputs and shape mismatches are surfaced as validation failures.
  - Schema/runtime binding or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [schema_case_validation.spec.md](/specs/03_conformance/cases/core/schema_case_validation.spec.md)
### schema_case_validation.doc.1.1.operator
- Location: `/specs/03_conformance/cases/core/schema_case_validation.spec.md`
- Summary: schema export validation case (operator)
- Description:
  Purpose: Capture actionable details on how this contract symbol behaves for the declared audience.
  
  Inputs:
  - Inputs come from the declared contract and export bindings for this symbol.
  - Runtime bindings and policy constraints are applied by the harness before evaluation.
  
  Returns:
  - The assertion or helper return value produced by this symbol.
  
  Errors/Caveats:
  - Malformed inputs and shape mismatches are surfaced as validation failures.
  - Schema/runtime binding or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### schema_case_validation.doc.2.1.operator
- Location: `/specs/03_conformance/cases/core/schema_case_validation.spec.md`
- Summary: schema export invalid imports case (operator)
- Description:
  Purpose: Capture actionable details on how this contract symbol behaves for the declared audience.
  
  Inputs:
  - Inputs come from the declared contract and export bindings for this symbol.
  - Runtime bindings and policy constraints are applied by the harness before evaluation.
  
  Returns:
  - The assertion or helper return value produced by this symbol.
  
  Errors/Caveats:
  - Malformed inputs and shape mismatches are surfaced as validation failures.
  - Schema/runtime binding or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### schema_case_validation.doc.3.1.operator
- Location: `/specs/03_conformance/cases/core/schema_case_validation.spec.md`
- Summary: missing status (operator)
- Description:
  Purpose: Capture actionable details on how this contract symbol behaves for the declared audience.
  
  Inputs:
  - Inputs come from the declared contract and export bindings for this symbol.
  - Runtime bindings and policy constraints are applied by the harness before evaluation.
  
  Returns:
  - The assertion or helper return value produced by this symbol.
  
  Errors/Caveats:
  - Malformed inputs and shape mismatches are surfaced as validation failures.
  - Schema/runtime binding or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### schema_case_validation.doc.4.1.operator
- Location: `/specs/03_conformance/cases/core/schema_case_validation.spec.md`
- Summary: invalid docs type (operator)
- Description:
  Purpose: Capture actionable details on how this contract symbol behaves for the declared audience.
  
  Inputs:
  - Inputs come from the declared contract and export bindings for this symbol.
  - Runtime bindings and policy constraints are applied by the harness before evaluation.
  
  Returns:
  - The assertion or helper return value produced by this symbol.
  
  Errors/Caveats:
  - Malformed inputs and shape mismatches are surfaced as validation failures.
  - Schema/runtime binding or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### schema_case_validation.doc.5.1.operator
- Location: `/specs/03_conformance/cases/core/schema_case_validation.spec.md`
- Summary: docs entry one (operator)
- Description:
  Purpose: Capture actionable details on how this contract symbol behaves for the declared audience.
  
  Inputs:
  - Inputs come from the declared contract and export bindings for this symbol.
  - Runtime bindings and policy constraints are applied by the harness before evaluation.
  
  Returns:
  - The assertion or helper return value produced by this symbol.
  
  Errors/Caveats:
  - Malformed inputs and shape mismatches are surfaced as validation failures.
  - Schema/runtime binding or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### schema_case_validation.doc.5.2.operator
- Location: `/specs/03_conformance/cases/core/schema_case_validation.spec.md`
- Summary: docs entry two (operator)
- Description:
  Purpose: Capture actionable details on how this contract symbol behaves for the declared audience.
  
  Inputs:
  - Inputs come from the declared contract and export bindings for this symbol.
  - Runtime bindings and policy constraints are applied by the harness before evaluation.
  
  Returns:
  - The assertion or helper return value produced by this symbol.
  
  Errors/Caveats:
  - Malformed inputs and shape mismatches are surfaced as validation failures.
  - Schema/runtime binding or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### schema_case_validation.doc.6.1.operator
- Location: `/specs/03_conformance/cases/core/schema_case_validation.spec.md`
- Summary: docs entry with unknown key (operator)
- Description:
  Purpose: Capture actionable details on how this contract symbol behaves for the declared audience.
  
  Inputs:
  - Inputs come from the declared contract and export bindings for this symbol.
  - Runtime bindings and policy constraints are applied by the harness before evaluation.
  
  Returns:
  - The assertion or helper return value produced by this symbol.
  
  Errors/Caveats:
  - Malformed inputs and shape mismatches are surfaced as validation failures.
  - Schema/runtime binding or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### schema_case_validation.doc.9.1.operator
- Location: `/specs/03_conformance/cases/core/schema_case_validation.spec.md`
- Summary: schema registry assertions yaml input (operator)
- Description:
  Purpose: Capture actionable details on how this contract symbol behaves for the declared audience.
  
  Inputs:
  - Inputs come from the declared contract and export bindings for this symbol.
  - Runtime bindings and policy constraints are applied by the harness before evaluation.
  
  Returns:
  - The assertion or helper return value produced by this symbol.
  
  Errors/Caveats:
  - Malformed inputs and shape mismatches are surfaced as validation failures.
  - Schema/runtime binding or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### schema_case_validation.doc.11.1.operator
- Location: `/specs/03_conformance/cases/core/schema_case_validation.spec.md`
- Summary: schema text export (operator)
- Description:
  Purpose: Capture actionable details on how this contract symbol behaves for the declared audience.
  
  Inputs:
  - Inputs come from the declared contract and export bindings for this symbol.
  - Runtime bindings and policy constraints are applied by the harness before evaluation.
  
  Returns:
  - The assertion or helper return value produced by this symbol.
  
  Errors/Caveats:
  - Malformed inputs and shape mismatches are surfaced as validation failures.
  - Schema/runtime binding or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [assertion_core.spec.md](/specs/05_libraries/conformance/assertion_core.spec.md)
### conf.json_type_is.doc.1.operator
- Location: `/specs/05_libraries/conformance/assertion_core.spec.md`
- Summary: Assert that a value is of the requested JSON schema type for operator workflows.
- Description:
  Purpose: Documents behavior for `conf.json_type_is` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### conformance.assertion.core.operator
- Location: `/specs/05_libraries/conformance/assertion_core.spec.md`
- Summary: Case `LIB-CONF-ASSERT-001` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `conf.json_type_is` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [chain_export_validation.spec.md](/specs/05_libraries/conformance/chain_export_validation.spec.md)
### bad.path.symbol.doc.1.operator
- Location: `/specs/05_libraries/conformance/chain_export_validation.spec.md`
- Summary: Validate chain-export failure signaling for an invalid or missing path symbol in operator workflows.
- Description:
  Purpose: Documents behavior for `bad.path.symbol` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### conformance.chain.export.validation.operator
- Location: `/specs/05_libraries/conformance/chain_export_validation.spec.md`
- Summary: Case `BAD-EXPORT-PATH` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `bad.path.symbol` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [artifact_core.spec.md](/specs/05_libraries/domain/artifact_core.spec.md)
### domain.artifact.write_yaml.doc.1.operator
- Location: `/specs/05_libraries/domain/artifact_core.spec.md`
- Summary: Persist a structured artifact payload as YAML for downstream consumers during operators.
- Description:
  Purpose: Documents behavior for `domain.artifact.write_yaml` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### domain.artifact.core.operator
- Location: `/specs/05_libraries/domain/artifact_core.spec.md`
- Summary: Case `LIB-DOMAIN-ARTIFACT-001-001-DOMAIN-ARTIFACT-WRITE-YAML` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `domain.artifact.write_yaml` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [conformance_core.spec.md](/specs/05_libraries/domain/conformance_core.spec.md)
### domain.conformance.validate_report_errors.doc.1.operator
- Location: `/specs/05_libraries/domain/conformance_core.spec.md`
- Summary: Validate conformance report errors and fail when reported issues violate expectations for operators.
- Description:
  Purpose: Documents behavior for `domain.conformance.validate_report_errors` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### domain.conformance.core.operator
- Location: `/specs/05_libraries/domain/conformance_core.spec.md`
- Summary: Case `LIB-DOMAIN-CONFORMANCE-001-000-DOMAIN-CONFORMANCE-ERROR-WHEN-FALSE` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `domain.conformance.validate_report_errors` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [contract_set_core.spec.md](/specs/05_libraries/domain/contract_set_core.spec.md)
### domain.contract_set.applies_to_runners.doc.1.operator
- Location: `/specs/05_libraries/domain/contract_set_core.spec.md`
- Summary: Evaluate whether a contract set should execute under the current runner for operator workflows.
- Description:
  Purpose: Documents behavior for `domain.contract_set.applies_to_runners` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### domain.contract_set.core.operator
- Location: `/specs/05_libraries/domain/contract_set_core.spec.md`
- Summary: Case `LIB-DOMAIN-CONTRACT-SET-001` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `domain.contract_set.applies_to_runners` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [fs_core.spec.md](/specs/05_libraries/domain/fs_core.spec.md)
### domain.fs.sort_spec_files.doc.1.operator
- Location: `/specs/05_libraries/domain/fs_core.spec.md`
- Summary: Sort discovered spec files into stable, deterministic ordering for operators.
- Description:
  Purpose: Documents behavior for `domain.fs.sort_spec_files` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### domain.fs.core.operator
- Location: `/specs/05_libraries/domain/fs_core.spec.md`
- Summary: Case `LIB-DOMAIN-FS-001-001-DOMAIN-FS-IS-DOCS-SPEC-FILE` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `domain.fs.sort_spec_files` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [http_core.spec.md](/specs/05_libraries/domain/http_core.spec.md)
### domain.http.step_status_is.doc.1.operator
- Location: `/specs/05_libraries/domain/http_core.spec.md`
- Summary: Assert that a HTTP workflow step reports the expected status for operators.
- Description:
  Purpose: Documents behavior for `domain.http.step_status_is` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### domain.http.core.operator
- Location: `/specs/05_libraries/domain/http_core.spec.md`
- Summary: Case `LIB-DOMAIN-HTTP-001-001-DOMAIN-HTTP-STATUS` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `domain.http.step_status_is` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [job_core.spec.md](/specs/05_libraries/domain/job_core.spec.md)
### domain.job.scan_bundle_has_result.doc.1.operator
- Location: `/specs/05_libraries/domain/job_core.spec.md`
- Summary: Verify job scan bundle output includes the expected result marker in operations.
- Description:
  Purpose: Documents behavior for `domain.job.scan_bundle_has_result` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### domain.job.core.operator
- Location: `/specs/05_libraries/domain/job_core.spec.md`
- Summary: Case `LIB-DOMAIN-JOB-001-000A-DOMAIN-JOB-SCAN-BUNDLE-HAS-RESULT` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `domain.job.scan_bundle_has_result` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [make_core.spec.md](/specs/05_libraries/domain/make_core.spec.md)
### make.has_target.doc.1.operator
- Location: `/specs/05_libraries/domain/make_core.spec.md`
- Summary: Check whether a Make target exists and is resolvable in operator workflows.
- Description:
  Purpose: Documents behavior for `make.has_target` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### domain.make.core.operator
- Location: `/specs/05_libraries/domain/make_core.spec.md`
- Summary: Case `LIB-DOMAIN-MAKE-001` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `make.has_target` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [markdown_core.spec.md](/specs/05_libraries/domain/markdown_core.spec.md)
### domain.markdown.tokens_all_present.doc.1.operator
- Location: `/specs/05_libraries/domain/markdown_core.spec.md`
- Summary: Assert required Markdown tokens appear in source markdown content for operators.
- Description:
  Purpose: Documents behavior for `domain.markdown.tokens_all_present` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### domain.markdown.core.operator
- Location: `/specs/05_libraries/domain/markdown_core.spec.md`
- Summary: Case `LIB-DOMAIN-MD-001-001-DOMAIN-MARKDOWN-HAS-HEADING` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `domain.markdown.tokens_all_present` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [meta_core.spec.md](/specs/05_libraries/domain/meta_core.spec.md)
### domain.meta.has_artifact_target.doc.1.operator
- Location: `/specs/05_libraries/domain/meta_core.spec.md`
- Summary: Detect whether contract metadata declares an artifact target for operations.
- Description:
  Purpose: Documents behavior for `domain.meta.has_artifact_target` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### domain.meta.core.operator
- Location: `/specs/05_libraries/domain/meta_core.spec.md`
- Summary: Case `LIB-DOMAIN-META-001-001-DOMAIN-META-CASE-ID-EQ` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `domain.meta.has_artifact_target` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [os_core.spec.md](/specs/05_libraries/domain/os_core.spec.md)
### domain.os.exec_ok.doc.1.operator
- Location: `/specs/05_libraries/domain/os_core.spec.md`
- Summary: Assert that an OS command execution completed successfully for operator workflows.
- Description:
  Purpose: Documents behavior for `domain.os.exec_ok` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### domain.os.core.operator
- Location: `/specs/05_libraries/domain/os_core.spec.md`
- Summary: Case `LIB-DOMAIN-OS-001-001-DOMAIN-OS-EXEC-OK` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `domain.os.exec_ok` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [path_core.spec.md](/specs/05_libraries/domain/path_core.spec.md)
### domain.path.sorted.doc.1.operator
- Location: `/specs/05_libraries/domain/path_core.spec.md`
- Summary: Return path inputs sorted in deterministic lexicographic order for operators.
- Description:
  Purpose: Documents behavior for `domain.path.sorted` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### domain.path.core.operator
- Location: `/specs/05_libraries/domain/path_core.spec.md`
- Summary: Case `LIB-DOMAIN-PATH-001-001-DOMAIN-PATH-NORMALIZE` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `domain.path.sorted` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [php_core.spec.md](/specs/05_libraries/domain/php_core.spec.md)
### php.is_assoc_projection.doc.1.operator
- Location: `/specs/05_libraries/domain/php_core.spec.md`
- Summary: Assert a PHP projection result is associative for operations.
- Description:
  Purpose: Documents behavior for `php.is_assoc_projection` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### domain.php.core.operator
- Location: `/specs/05_libraries/domain/php_core.spec.md`
- Summary: Case `LIB-DOMAIN-PHP-001` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `php.is_assoc_projection` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [process_core.spec.md](/specs/05_libraries/domain/process_core.spec.md)
### domain.process.exec_capture_ex_code.doc.1.operator
- Location: `/specs/05_libraries/domain/process_core.spec.md`
- Summary: Capture and assert process exit/exception code behavior for operators.
- Description:
  Purpose: Documents behavior for `domain.process.exec_capture_ex_code` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### domain.process.core.operator
- Location: `/specs/05_libraries/domain/process_core.spec.md`
- Summary: Case `LIB-DOMAIN-PROCESS-001-001-DOMAIN-PROCESS-EXEC-CAPTURE-EX-CODE` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `domain.process.exec_capture_ex_code` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [python_core.spec.md](/specs/05_libraries/domain/python_core.spec.md)
### py.is_tuple_projection.doc.1.operator
- Location: `/specs/05_libraries/domain/python_core.spec.md`
- Summary: Assert that a Python projection result is tuple-shaped as expected for operators.
- Description:
  Purpose: Documents behavior for `py.is_tuple_projection` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### domain.python.core.operator
- Location: `/specs/05_libraries/domain/python_core.spec.md`
- Summary: Case `LIB-DOMAIN-PY-001` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `py.is_tuple_projection` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [repo_core.spec.md](/specs/05_libraries/domain/repo_core.spec.md)
### domain.repo.walk_matching.doc.1.operator
- Location: `/specs/05_libraries/domain/repo_core.spec.md`
- Summary: Walk repository paths and return files matching configured selectors for operators.
- Description:
  Purpose: Documents behavior for `domain.repo.walk_matching` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### domain.repo.core.operator
- Location: `/specs/05_libraries/domain/repo_core.spec.md`
- Summary: Case `LIB-DOMAIN-REPO-001-001-DOMAIN-REPO-WALK-MATCHING` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `domain.repo.walk_matching` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [yaml_core.spec.md](/specs/05_libraries/domain/yaml_core.spec.md)
### domain.yaml.stringify.doc.1.operator
- Location: `/specs/05_libraries/domain/yaml_core.spec.md`
- Summary: Serialize structured data into canonical YAML text for operators.
- Description:
  Purpose: Documents behavior for `domain.yaml.stringify` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### domain.yaml.core.operator
- Location: `/specs/05_libraries/domain/yaml_core.spec.md`
- Summary: Case `LIB-DOMAIN-YAML-001-001-DOMAIN-YAML-PARSE-GET-OR` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `domain.yaml.stringify` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [path_core.spec.md](/specs/05_libraries/path/path_core.spec.md)
### path.matches.doc.1.operator
- Location: `/specs/05_libraries/path/path_core.spec.md`
- Summary: Evaluate whether path values satisfy the provided matcher rules for operators.
- Description:
  Purpose: Documents behavior for `path.matches` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### path.path.core.operator
- Location: `/specs/05_libraries/path/path_core.spec.md`
- Summary: Case `LIB-PATH-001-001-PATH-NORMALIZE-SLASHES` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `path.matches` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [policy_core.spec.md](/specs/05_libraries/policy/policy_core.spec.md)
### policy.violation_count_is.doc.1.operator
- Location: `/specs/05_libraries/policy/policy_core.spec.md`
- Summary: Assert the exact number of policy violations for operators.
- Description:
  Purpose: Documents behavior for `policy.violation_count_is` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### policy.policy.core.operator
- Location: `/specs/05_libraries/policy/policy_core.spec.md`
- Summary: Case `LIB-POLICY-001` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `policy.violation_count_is` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

## [policy_metrics.spec.md](/specs/05_libraries/policy/policy_metrics.spec.md)
### policy.metric_non_increase.doc.1.operator
- Location: `/specs/05_libraries/policy/policy_metrics.spec.md`
- Summary: Assert a policy metric does not increase compared to a baseline for operators.
- Description:
  Purpose: Documents behavior for `policy.metric_non_increase` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.

### policy.policy.metrics.operator
- Location: `/specs/05_libraries/policy/policy_metrics.spec.md`
- Summary: Case `LIB-POLICY-002-001-POLICY-METRIC-NON-DECREASE` for `contract.export`. (operator)
- Description:
  Purpose: Documents behavior for `policy.metric_non_increase` for this audience, including input contract, output contract, and failure modes.
  
  Inputs:
  - Inputs are the fixture inputs bound to this docs-bearing symbol as declared in the owning export or case entry.
  - Runtime bindings and policy constraints are applied by the harness before execution. 
  
  Returns:
  - Returns the assertion/helper value emitted by this symbol after bindings and policy checks execute.
  
  Errors/Caveats:
  - Input/shape mismatches are surfaced as validation failures. 
  - Schema, runtime binding, or environment mismatches are surfaced as runtime or validation failures.
  - Policy and validation failures are surfaced through contract evaluation, including policy assertions and policy checks.
  
  Usage context:
  - Use this for observability, runbook readiness, and incident response.



## [stdlib_string_reference.spec.md](/specs/05_libraries/domain/stdlib_string_reference.spec.md)
### std.string.contains.doc.1.operator
- Location: `/specs/05_libraries/domain/stdlib_string_reference.spec.md`
- Summary: Route operational checks using substring membership.
- Description:
  Purpose: Drive operational routing and runbook branches with deterministic string matching.

  Inputs:
  - haystack: Input text being searched.
  - needle: Search token. Empty token is treated as present.

  Returns:
  - Boolean true when token exists, false otherwise.

  Errors/Caveats:
  - Type or schema validation failures before predicate execution.

  Usage context:
  - operator

### std.string.starts_with.doc.1.operator
- Location: `/specs/05_libraries/domain/stdlib_string_reference.spec.md`
- Summary: Use prefix checks to route structured operations.
- Description:
  Purpose: Drive operational routing and classification workflows by prefix checks.

  Inputs:
  - value: Candidate text.
  - prefix: Prefix token.

  Returns:
  - Boolean true when the value starts with the prefix.

  Errors/Caveats:
  - Type mismatch or arity errors.

  Usage context:
  - Prefix membership checks in text-heavy workflows.

### std.string.ends_with.doc.1.operator
- Location: `/specs/05_libraries/domain/stdlib_string_reference.spec.md`
- Summary: Route operations by deterministic suffix checks.
- Description:
  Purpose: Drive operational routing and control checks by suffix checks.

  Inputs:
  - value: Candidate text.
  - suffix: Suffix token.

  Returns:
  - Boolean true when the value ends with the suffix.

  Errors/Caveats:
  - Type mismatch or shape validation failures.

  Usage context:
  - Trailing token checks for operational or policy routing.
