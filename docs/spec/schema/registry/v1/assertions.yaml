version: 1
id: schema.registry.v1.assertions
type: assertions
fields:
  contract:
    type: list
    required: false
    since: v1
    description: Assertion tree root; leaves are direct spec-lang operator mappings on core executable surfaces.
