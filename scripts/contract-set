#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

echo "WARN: scripts/contract-set is deprecated; use scripts/bundle instead." >&2
exec "${ROOT_DIR}/scripts/bundle" "$@"
